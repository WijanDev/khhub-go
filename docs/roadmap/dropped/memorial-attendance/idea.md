# Memorial attendance

- **Slug:** memorial-attendance
- **Status:** dropped
- **Merge date:**
- **App version:**

## Summary

Dropped: Memorial attendance is in `attendance-duplicate-guard` (own `kind`, dated exception, not a fake weekend row). Do not keep a second Memorial idea.

## Out of scope

- Re-opening this slug unless the calendar idea is split again.
