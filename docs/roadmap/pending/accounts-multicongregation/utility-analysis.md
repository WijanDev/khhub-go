# Utility analysis

- **Score:** 5
- **Scale:** 1 = low … 5 = high

## Justification

Without this, only one admin can use khhub and only one congregation fits the schema. Publisher login, stacked privileges, and a second congregation are blocked. The secretary’s weekly job stays a bottleneck; the product direction (program, assignments, own report) cannot ship safely. Skipping it keeps the current single-user MVP. That is a hard constraint for the approved design, so utility is 5.
