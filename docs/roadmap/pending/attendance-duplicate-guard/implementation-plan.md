# Implementation plan

1. Add a unique constraint (migration + sqlc) on meeting date + type.
2. Return a clear JSON error from the attendance handler.
2. Show Spanish copy on the form. Add a handler test.
