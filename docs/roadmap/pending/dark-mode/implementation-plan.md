# Implementation plan

1. Confirm `base-nova` / Tailwind dark tokens. Toggle in the shell; persist `localStorage`.
2. Check login, tables, and cards in both themes.
3. Verify: `npm run lint && npm run build`; browser desktop and a narrow viewport.
