# Implementation plan

1. Pick `go vet` + `gofmt -l` or a pinned golangci-lint config.
2. Add the step to `.github/workflows/ci.yml` and document the local command in `README.md`.
2. Fix any first-run findings.
