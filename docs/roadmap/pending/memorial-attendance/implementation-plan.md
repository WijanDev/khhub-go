# Implementation plan

1. Attendance `kind` adds `memorial` or a dedicated table `(congregation_id, memorial_date, in_person, online)`. Unique per congregation and date.
2. SPA form on attendance or a Memorial page. Dashboard can show the latest Memorial total.
3. Verify: cannot insert two Memorials the same date; midweek/weekend unchanged; browser.
