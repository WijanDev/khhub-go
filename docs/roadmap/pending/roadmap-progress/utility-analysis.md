# Utility analysis

- **Score:** 3
- **Scale:** 1 = low … 5 = high

## Justification

The secretary never opens this file. The operator and agents do, every time they pick the next idea. Without counts it is easy to treat a 40-row pending list as “almost done” or to forget how much is already on `dev`. Target versions later stop mixing “someday” with “this release.” Skipping it leaves the same ranked tables; we just keep counting by hand.
