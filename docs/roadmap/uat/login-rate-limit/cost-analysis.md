# Cost analysis

- **Score:** 2
- **Scale:** 1 = cheap … 5 = expensive
- **Ratio (utility ÷ cost):** 2.00

## Justification

Localized to the login handler (steps 1–2). About a day plus tests. No schema required if in-memory + one instance; a table if we want multi-replica later.
